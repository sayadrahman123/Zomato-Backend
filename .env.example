# .env file — local development defaults
# DO NOT commit this file to git (it's in .gitignore)

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=zomato_db
DB_USERNAME=root
DB_PASSWORD=root

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT
JWT_SECRET=zomato_super_secret_jwt_key_change_this_in_production_2024
JWT_EXPIRATION_MS=86400000

# Spring
SPRING_PROFILES_ACTIVE=dev
